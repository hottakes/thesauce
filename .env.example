# =============================================================================
# Environment Variables Configuration
# =============================================================================
# Copy this file to .env.local and fill in the values.
# For local development, run: npm run env:setup (requires 1Password CLI)
# =============================================================================

# Supabase Configuration
# Get these from 1Password vault "Engineering-Staging/sauce-web" for local dev
# Or from your Supabase project settings -> API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# App Configuration
# Set this to your app's URL (used for auth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Environment identifier
# Values: development | staging | production
NEXT_PUBLIC_APP_ENV=development
